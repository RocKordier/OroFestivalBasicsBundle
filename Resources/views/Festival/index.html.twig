{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set gridName = 'ehdev-festival-festival-grid' %}
{% set pageTitle = 'ehdev.festivalbasics.festival.entity_label'|trans %}

{% block navButtons %}
    {% if is_granted('ehdev_festival_festival_create') %}
        {% set dialogTitle = 'ehdev.festivalbasics.festival.create_entity'|trans %}
        {{ UI.clientButton({
            'dataUrl': path('ehdev_festival_festival_create'),
            'aCss': 'no-hash',
            'iCss': 'fa-plus',
            'dataId': null,
            'title' : dialogTitle,
            'label' : dialogTitle,
            'widget' : {
                'type' : 'dialog',
                'multiple' : false,
                'reload-grid-name' : gridName,
                'options' : {
                    'stateEnabled': false,
                    'dialogOptions' : {
                        'title' : dialogTitle,
                        'allowMaximize': true,
                        'allowMinimize': true,
                        'dblclick': 'maximize',
                        'maximizedHeightDecreaseBy': 'minimize-bar',
                        'width': 500
                    }
                }
            }
        })
        }}
    {% endif %}
{% endblock %}
