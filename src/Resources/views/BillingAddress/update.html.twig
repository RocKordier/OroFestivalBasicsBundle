{% extends '@EHDevBasics/Widgets/formWidget.html.twig' %}

{% set pathCreate = path('ehdev_festival_billingaddress_create', {id: festivalAccount.id}) %}
{% if form.vars.value.id is not null %}
    {% set pathUpdate = path('ehdev_festival_billingaddress_update', { id: form.vars.value.id}) %}
{% endif %}

{% block requireAction %}
    widgetManager.getWidgetInstance('festival-account-festivals-widget', function (widget) {
        widget.render();
    });
{% endblock %}
