{% extends '@OroUI/actions:index.html.twig' %}
{% import '@OroUI/macros.html.twig' as UI %}

{% set gridName = 'ehdev-festival-stage-grid' %}
{% set pageTitle = 'ehdev.festivalbasics.stage.entity_label'|trans %}

{% block navButtons %}
    {% if is_granted('ehdev_festival_stage_create') %}
        {% set dialogTitle = 'ehdev.festivalbasics.stage.create_entity'|trans %}
        {{ UI.clientButton({
            'dataUrl': path('ehdev_festival_stage_create'),
            'aCss': 'no-hash',
            'iCss': 'fa-plus',
            'dataId': null,
            'title' : dialogTitle,
            'label' : dialogTitle,
            'widget' : {
                'type' : 'dialog',
                'multiple' : false,
                'reload-grid-name' : gridName,
                'options' : {
                    'stateEnabled': false,
                    'dialogOptions' : {
                        'title' : dialogTitle,
                        'allowMaximize': true,
                        'allowMinimize': true,
                        'dblclick': 'maximize',
                        'maximizedHeightDecreaseBy': 'minimize-bar',
                        'width': 500
                    }
                }
            }
        })
        }}
    {% endif %}
{% endblock %}
