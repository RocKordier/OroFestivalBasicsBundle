{% extends '@EHDevBasics/Widgets/formWidget.html.twig' %}

{% set pathCreate = path('ehdev_festival_festival_create') %}
{% if form.vars.value.id is not null %}
    {% set pathUpdate = path('ehdev_festival_festival_update', { id: form.vars.value.id}) %}
{% endif %}

{% block requireAction %}
    widgetManager.getWidgetInstance('festival-info-widget', function (widget) {
        widget.render();
    });
{% endblock %}
