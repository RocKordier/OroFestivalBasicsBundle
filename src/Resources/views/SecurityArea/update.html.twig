{% extends '@EHDevBasics/Widgets/formWidget.html.twig' %}

{% set pathCreate = path('ehdev_festival_security_area_create') %}
{% if form.vars.value.id is not null %}
    {% set pathUpdate = path('ehdev_festival_security_area_update', { id: form.vars.value.id}) %}
{% endif %}
